import React from 'react';
import '../CSS/OS.scss';

function Manuals() {
    return (
        <div className={'window fill'}>
            <h1 className={'App-header'}>Manuals</h1>
            <p className={'message App-header'}>⚠️Under Construction ⚠️</p>
            <input type="text" className={'message input'}/>
        </div>
    );
}

export default Manuals;